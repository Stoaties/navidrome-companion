fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
httpx==0.28.1
mutagen==1.47.0
# Note: yt-dlp is intentionally NOT here. It is installed into an isolated pipx
# environment in the Dockerfile and invoked as a subprocess.
